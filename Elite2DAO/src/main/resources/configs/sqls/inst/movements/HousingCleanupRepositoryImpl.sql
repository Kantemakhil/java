OFFENDER_DETAILS {
SELECT OFFENDER_ID,LAST_NAME,FIRST_NAME,BIRTH_DATE,OFFENDER_ID_DISPLAY,ROOT_OFFENDER_ID,AGE
FROM OFFENDERS
WHERE FIRST_NAME IN (:parameters) and BIRTH_DATE=:dob
}

OFFENDER_DET {
SELECT OFFENDER_ID,LAST_NAME,FIRST_NAME,BIRTH_DATE,OFFENDER_ID_DISPLAY,ROOT_OFFENDER_ID,AGE,OFFENDER_BOOK_ID
FROM V_HEADER_BLOCK_FN(:USERID) v_header_block
WHERE FIRST_NAME IN (:parameters) and BIRTH_DATE=:dob
}

FLOOR_PLAN_DET{
SELECT FLOOR_PLAN_ID FROM FLOOR_PLAN
}
MAX_IMAGE_ID {
SELECT MAX(IMAGE_ID) FROM IMAGES
}
INSERT_IMAGE{
INSERT INTO images VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)
}
UPDATE_FLOORPLAN {
UPDATE FLOOR_PLAN SET IMAGE_ID = ? WHERE FLOOR_PLAN_ID = ?
}
MAX_IMAGE_ID_FLOORPLAN{
SELECT MAX(IMAGE_ID) FROM FLOOR_PLAN
}

FETCH_LOCATIONS_COUNT {
select count(*) from agency_locations where agy_loc_id='STAG'
}

ASSIGN_DEFUALT_LOCATION_TO_OFFENDERS {
update offender_bookings 
set LIVING_UNIT_ID=6095 where offender_book_id in (
select offender_book_id from V_HEADER_BLOCK_FN(:USERID) v_header_block where BIRTH_DATE != '15-AUG-1973' and agy_loc_id='STAG')
}